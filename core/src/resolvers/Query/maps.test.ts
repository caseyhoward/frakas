import maps from "./maps";
import * as Models from "../../Models";
import * as Repository from "../../Repository";

describe("Query.map", () => {
  it("gets a map", async () => {
    // const repository = await TestRepository.repository;
    // const map = await Factories.createMap();
    // const allMaps = await maps(repository.findAllMaps);
    // expect(allMaps).toContainEqual(Models.mapToGraphql(map));
  });
});
