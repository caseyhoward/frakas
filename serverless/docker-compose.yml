version: "3"

services:
  dynamodb:
    image: amazon/dynamodb-local
    ports:
      - 8000:8000
    command: ["-jar", "DynamoDBLocal.jar", "-inMemory", "-sharedDb"]
    volumes:
      - "$HOME/.aws:/home/dynamodblocal/.aws"